PORT=3001

# Database Configuration (update these for your PostgreSQL instance)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=omnirambles
DB_USER=postgres
DB_PASSWORD=your_password_here

# Ollama Configuration (should work if Ollama is running locally)
OLLAMA_BASE_URL=http://localhost:11435
